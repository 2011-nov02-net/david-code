@*
    @ followed by a C# expression is rendered by evaluation the express, converting the result to a string,
    and inserting that onto the HTML Document

    @ followed by a c# block of some kind - like for () {} , if() {}. or just {}
    adds code that will run during the render of the view
*@

@{ 
    // setting the layout propery visible from the view will give the view a layout.
    Layout = "_Layout";
}

@{
    var x = 123;
}

@x

@{ 
    x = 321;
}

@x
